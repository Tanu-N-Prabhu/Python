name: Update Version History

on:
  push:
    branches:
      - main      # Change if needed
  workflow_dispatch: # optional manual run

jobs:
  update-version-history:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Run version history updater
        run: python update_versions.py

      - name: Commit & push updated VERSION_HISTORY.md
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add VERSION_HISTORY.md
          git commit -m "Auto-update version history" || echo "No changes to commit"
          git push
